# VoiceBee sidecar — direct dependencies (Phase 2, DECISIONS Q12).
# Built on the base system Python (3.14+); create the env with:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# The .venv is this app's isolated dependency folder, not a separate/legacy
# Python — it always runs on whatever the base `python3` is.

mlx-whisper==0.4.3      # local streaming STT on Apple Silicon (MLX)
sounddevice==0.5.5      # mic capture for the talk.py / live.py Mac demos
mlx-audio==0.4.4        # cloned-voice TTS (Chatterbox zero-shot) on Apple Silicon

# Realtime voice (P5 — the iOS WebRTC client). The [webrtc] extra adds aiortc +
# PyAV + pylibsrtp + opencv (P2P SmallWebRTC transport, no cloud SFU); [silero]
# adds the VAD used for realtime turn-taking + native barge-in. All ship arm64
# wheels for Python 3.14 (verified — no source builds). websockets is a base
# transitive dep not pulled by the narrow pin. NOTE: av and opencv each bundle
# their own ffmpeg dylibs (a harmless objc duplicate-class warning for our
# audio-only path) — dedupe when bundling into the signed runtime (#4c).
pipecat-ai[webrtc,silero]==1.3.0   # realtime pipeline: SmallWebRTC + VAD/STT/LLM/TTS
websockets==16.0

# Dev/test only — the Pipecat runner + prebuilt web client used to validate the
# realtime pipeline from a browser (`python realtime.py -t webrtc`). The daemon
# path (answer_offer) relays signaling itself and does NOT need these in prod.
fastapi==0.137.1
uvicorn==0.49.0
pipecat-ai-prebuilt==1.0.2
