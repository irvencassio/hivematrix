# DeepSeek Python Game Reliability Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

- [x] Add a failing test in `src/lib/orchestrator/generic-agent.test.ts` asserting the local developer system prompt includes DeepSeek/DwarfStar agent posture, Python game dependency guidance, and final-verification wording.
- [x] Update `src/lib/orchestrator/generic-agent.ts` to append a concise local-agent deliverable reliability block to tool-enabled profiles.
- [x] Repair `/Users/irvcassio/Documents/inbox/snake.py` to avoid `pygame` entirely so the existing task can run under the installed Python 3.14.6 state.
- [x] Run the focused test, then smoke-test `snake.py` without any venv.
