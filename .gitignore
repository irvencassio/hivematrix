node_modules/
dist/
build/
.next/
**/.build/
*.db
*.db-wal
*.db-shm
.env
.env.local
.DS_Store
.rapidmlx-eval/
__pycache__/
*.pyc
.Codex-resumes/
# Task-branch worktree container (machine-local scratch created by the task
# runner inside the repo). Untracked, it makes the tree read "dirty" and blocks
# the release script's clean-tree gate while any task is in flight.
.hive-worktrees/
